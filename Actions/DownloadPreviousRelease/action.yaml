name: Download Previous Release
author: Microsoft Corporation
inputs:
  shell:
    description: Shell in which you want to run the action (powershell or pwsh)
    required: false
    default: powershell
  token:
    description: The GitHub token running the action
    required: false
    default: ${{ github.token }}
  project:
    description: Project folder
    required: false
    default: '.'
outputs:
  PreviousAppsPath:
    description: Path to the folder containing the downloaded previous release apps. Empty if no release was found.
    value: ${{ steps.DownloadPreviousRelease.outputs.PreviousAppsPath }}
runs:
  using: composite
  steps:
    - name: run
      shell: ${{ inputs.shell }}
      id: DownloadPreviousRelease
      env:
        _token: ${{ inputs.token }}
        _project: ${{ inputs.project }}
      run: |
        ${{ github.action_path }}/../Invoke-AlGoAction.ps1 -ActionName "DownloadPreviousRelease" -Action {
          ${{ github.action_path }}/DownloadPreviousRelease.ps1 -token $ENV:_token -project $ENV:_project
        }
branding:
  icon: terminal
  color: blue
