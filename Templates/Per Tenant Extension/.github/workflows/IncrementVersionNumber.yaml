name: Increment Version Number

on:
  workflow_dispatch:
    inputs:
      versionNumber:
        description: New Version Number (Major.Minor)
        required: true
      directCommit:
        description: Direct COMMIT (Y/N)
        required: false
        default: 'N'

defaults:
  run:
    shell: PowerShell

jobs:
  initialization :
    runs-on: windows-latest
    outputs:
      telemetryScope: ${{ steps.init.outputs.telemetryScope }}
      correlationId: ${{ steps.init.outputs.correlationId }}  
    steps:
      - id: init
        uses: microsoft/AL-Go/InitWorkflow@main
        with: 
          workflowName : "DO0096"

  update:
    runs-on: windows-latest
    needs : [ initialization ]

    steps:
      - name: Increment Version Number
        uses: microsoft/AL-Go/IncrementVersionNumber@main
        with:
          parentCorrelationId: ${{ needs.initialization.outputs.correlationId }}
          versionNumber: ${{ github.event.inputs.versionNumber }}
          directCommit: ${{ github.event.inputs.directCommit }}
